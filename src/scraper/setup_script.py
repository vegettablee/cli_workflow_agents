# gets the downloaded cookies from a directory and then injects them into the chrome debug instance running
# so the steps for this are: 
# 1. check directory for downloaded cookies 
# 2. check if chrome debug instance is running
# 3. inject them into the chrome debug instance running(is still on a blank page)
# 4. notify scraper state 

