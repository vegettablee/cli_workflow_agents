# state for everything scraper related 
# refreshes every x seconds, needs to have a flag for when refreshes are happening to prevent clashes 
# with the chrome tools 

# for agent progress/visibility
# probably also for stats about the current session 
# also for managing the chrome debug session open, so for basic navigation, downloading html, etc. 